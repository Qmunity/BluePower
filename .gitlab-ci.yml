image: java:8-jdk

cache:
  key: ${CI_JOB_NAME}
variables:
  GRADLE_OPTS: "-Dorg.gradle.daemon=true"

build:
  stage: build
  script:
    - chmod +x gradlew
    - ./gradlew build
  artifacts:
    paths:
      - build/libs/*.jar
